"""Tailwind CSS theme app configuration."""

from django.apps import AppConfig


class ThemeConfig(AppConfig):
    """Configuration for the Tailwind theme app."""

    name = "theme"
    verbose_name = "Tailwind Theme"
